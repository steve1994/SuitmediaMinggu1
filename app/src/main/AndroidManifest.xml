<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.steve.tessuitmediaandroid" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".EnterNameActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuActivity_"
            android:label="@string/title_activity_main_menu" >
        </activity>
        <activity
            android:name=".EventActivity_"
            android:label="@string/title_event_toolbar">
        </activity>
        <activity
            android:name=".GuestActivity_"
            android:label="@string/title_activity_guest" >
        </activity>
    </application>

</manifest>
